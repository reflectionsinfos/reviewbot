"""GitHub / GitLab connector (PB-5).

Fetches: open issues, recent PR descriptions and review comments.
Processing: LLM summarises → embed in project_brain_{id}.
"""

# Implementation: Phase 6
# Skeleton only.


async def sync_github(project_id: str, github_repo: str) -> int:
    """Sync GitHub issues and PRs into project_brain_{project_id}.
    Phase 6 implementation.
    """
    raise NotImplementedError("GitHub connector — Phase 6")
