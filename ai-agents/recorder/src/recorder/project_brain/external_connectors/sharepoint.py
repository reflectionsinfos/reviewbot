"""SharePoint connector (PB-5).

Fetches .docx, .pdf, .md from SharePoint document libraries via Microsoft Graph API.
Processing: same pipeline as Document Indexer.
"""

# Implementation: Phase 6
# Skeleton only.


async def sync_sharepoint(project_id: str, library_url: str) -> int:
    """Sync SharePoint library into project_brain_{project_id}.
    Phase 6 implementation.
    """
    raise NotImplementedError("SharePoint connector — Phase 6")
