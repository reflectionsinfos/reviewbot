"""Confluence connector (PB-5).

Fetches pages in a Confluence space (respects permissions).
Processing: same pipeline as Document Indexer.
"""

# Implementation: Phase 6
# Skeleton only.


async def sync_confluence(project_id: str, space_key: str) -> int:
    """Sync Confluence space into project_brain_{project_id}.
    Phase 6 implementation.
    """
    raise NotImplementedError("Confluence connector — Phase 6")
